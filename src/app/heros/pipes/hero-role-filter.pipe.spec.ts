import { HeroRoleFilterPipe } from './hero-role-filter.pipe';

describe('HeroRoleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroRoleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
