import { HeroHpFilterPipe } from './hero-hp-filter.pipe';

describe('HeroHpFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroHpFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
